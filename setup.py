from setuptools import setup

setup(name="rgbled",
      version="1.0.0",
      author="Kevin Morris",
      author_email="kevr@0cost.org",
      packages=["rgbled"],
      scripts=["scripts/rgbled"])
